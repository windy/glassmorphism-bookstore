<div class="container mx-auto px-4 py-8 relative z-10">
  <div class="max-w-2xl mx-auto text-center">
    <!-- 失败动画图标 -->
    <div class="mb-8">
      <div class="text-8xl mb-4 floating">❌</div>
      <div class="glass rounded-full w-32 h-32 mx-auto flex items-center justify-center">
        <span class="text-4xl text-white">😔</span>
      </div>
    </div>

    <!-- 失败消息 -->
    <div class="glass rounded-3xl p-8 mb-8">
      <h1 class="text-4xl font-bold text-white text-glow mb-4">
        支付失败
      </h1>
      
      <p class="text-xl text-white/90 mb-6">
        很抱歉，您的支付未能完成
      </p>

      <!-- 订单信息 -->
      <div class="card-glass shadow-lg mb-6">
        <div class="card-body">
          <h2 class="text-2xl font-bold text-white mb-4">订单信息</h2>
          <div class="space-y-3 text-left">
            <div class="flex justify-between items-center">
              <span class="text-white/70">订单号</span>
              <span class="font-bold text-white">#<%= @order.id %></span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-white/70">商品</span>
              <span class="text-white"><%= @order.product_name %></span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-white/70">应付金额</span>
              <span class="text-2xl font-bold text-white glow">¥<%= @order.amount %></span>
            </div>
            <div class="flex justify-between items-center">
              <span class="text-white/70">订单状态</span>
              <div class="badge badge-error">支付失败</div>
            </div>
          </div>
        </div>
      </div>

      <!-- 可能的原因 -->
      <div class="alert alert-warning mb-6">
        <div class="text-left">
          <h3 class="font-semibold text-white mb-2">可能的原因：</h3>
          <ul class="text-white/80 space-y-1">
            <li>• 银行卡余额不足</li>
            <li>• 支付信息输入错误</li>
            <li>• 网络连接问题</li>
            <li>• 银行卡被冻结或限制</li>
          </ul>
        </div>
      </div>
    </div>

    <!-- 操作按钮 -->
    <div class="flex flex-col sm:flex-row gap-4 justify-center mb-8">
      <%= link_to pay_order_path(@order), method: :post,
                  class: "btn btn-glass btn-lg border-glow text-white pulse-glow" do %>
        <span class="mr-2">🔄</span> 重新支付
      <% end %>
      
      <%= link_to order_path(@order), class: "btn btn-glass btn-lg text-white" do %>
        <span class="mr-2">📋</span> 查看订单详情
      <% end %>
      
      <%= link_to cart_path, class: "btn btn-glass btn-lg text-white" do %>
        <span class="mr-2">🛒</span> 返回购物车
      <% end %>
    </div>

    <!-- 帮助信息 -->
    <div class="glass rounded-xl p-6">
      <h3 class="text-lg font-semibold text-white mb-3">
        <span class="mr-2">💡</span> 需要帮助？
      </h3>
      <div class="text-white/80 text-left space-y-2">
        <p>• 请检查您的银行卡信息是否正确</p>
        <p>• 确认银行卡有足够的余额</p>
        <p>• 如果问题持续存在，请联系您的银行或我们的客服</p>
        <p>• 您也可以选择其他支付方式</p>
      </div>
      
      <div class="mt-4">
        <%= link_to books_path, class: "btn btn-ghost text-white" do %>
          <span class="mr-2">←</span> 继续浏览商品
        <% end %>
      </div>
    </div>
  </div>
</div>