<div class="hero min-h-screen bg-base-200">
  <div class="hero-content flex-col">
    <div class="card w-full max-w-sm shadow-2xl bg-base-100 min-w-96">
      <div class="card-body">
        <h1 class="text-3xl font-bold text-center">Sign in</h1>

        <%= form_with(url: sign_in_path, class: "space-y-4") do |form| %>
          <div class="form-control">
            <%= form.label :email, class: "label" do %>
              <span class="label-text">Email</span>
            <% end %>
            <%= form.email_field :email, value: params[:email_hint], required: true, autofocus: true, autocomplete: "email", class: "input input-bordered w-full" %>
          </div>

          <div class="form-control">
            <%= form.label :password, class: "label" do %>
              <span class="label-text">Password</span>
            <% end %>
            <%= form.password_field :password, required: true, autocomplete: "current-password", class: "input input-bordered w-full" %>
          </div>

          <div class="form-control mt-6">
            <%= form.submit "Sign in", class: "btn btn-primary w-full" %>
          </div>
        <% end %>

        <div class="divider">OR</div>

        <%= button_to "Sign in with OmniAuth", "/auth/developer", class: "btn btn-outline w-full" %>

        <div class="text-center mt-4 space-y-2">
          <div>
            <%= link_to "Sign up", sign_up_path, class: "link link-primary" %>
          </div>
          <div>
            <%= link_to "Forgot your password?", new_identity_password_reset_path, class: "link link-secondary" %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
