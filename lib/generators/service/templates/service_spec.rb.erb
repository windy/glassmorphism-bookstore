require 'rails_helper'

RSpec.describe <%= class_name %>, type: :service do
  describe '#call' do
    it 'can be initialized and called' do
      service = <%= class_name %>.new
      expect { service.call }.not_to raise_error
    end
  end
end
