<div class="container mx-auto px-4 py-8 max-w-2xl">
  <div class="bg-white shadow-xl rounded-lg overflow-hidden">
    <div class="bg-gradient-to-r from-red-500 to-pink-600 px-8 py-12 text-center">
      <h1 class="text-4xl font-bold text-white mb-2">Payment Failed</h1>
      <p class="text-xl text-red-100">Your payment could not be processed</p>
    </div>

    <div class="px-8 py-6">
      <div class="text-center mb-6">
        <h2 class="text-2xl font-semibold text-gray-900 mb-2">Order #<%%= @order.id %></h2>
        <p class="text-gray-600">Don't worry, you haven't been charged</p>
      </div>

      <div class="bg-red-50 border border-red-200 rounded-lg p-6 mb-6">
        <h3 class="font-semibold text-red-900 mb-3">Possible Reasons for Payment Failure</h3>
        <ul class="text-red-800 space-y-2">
          <li>• Insufficient funds in your account</li>
          <li>• Card was declined by your bank</li>
          <li>• Incorrect card information entered</li>
          <li>• Network or connection issue during payment</li>
        </ul>
      </div>

      <div class="bg-gray-50 rounded-lg p-6 mb-6">
        <h3 class="font-semibold text-gray-900 mb-4">Order Details</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <div class="space-y-2">
              <div>
                <span class="text-sm text-gray-500">Name:</span>
                <p class="font-medium"><%%= @order.customer_name %></p>
              </div>
              <div>
                <span class="text-sm text-gray-500">Email:</span>
                <p class="font-medium"><%%= @order.customer_email %></p>
              </div>
            </div>
          </div>
          
          <div>
            <div class="space-y-2">
              <div>
                <span class="text-sm text-gray-500">Product:</span>
                <p class="font-medium"><%%= @order.product_name %></p>
              </div>
              <div>
                <span class="text-sm text-gray-500">Amount:</span>
                <p class="font-bold text-lg text-gray-900"><%%= @order.formatted_amount %></p>
              </div>
            </div>
          </div>
        </div>
      </div>

      <div class="flex flex-col sm:flex-row gap-4 justify-center">
        <%%= link_to "Try Payment Again", new_order_path, class: "btn btn-primary btn-lg" %>
        <%%= link_to "View Order Details", @order, class: "btn btn-outline" %>
      </div>
    </div>
  </div>
</div>